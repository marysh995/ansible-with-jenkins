- name: "manage worker 1"
  hosts: "web"
  become: true
  tasks:
   - name: "install maven"
     apt:
        - name: "maven"
          state: present
          update_cache: yes
          cache_valid_time: 3600
          
              

 
